[package]
name = "dream-compiler"
version = "0.1.0"
edition = "2021"
description = "Compiles kids' dreams into voxel structures — describe it, we build it"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
